/*
# core-loop Module

This module, `core-loop`, is designed to provide the base loop of promting the llm and executing tools.

## Structure

- **src/main/java**: Contains the main Java source files for the `core-loop` module.

## Usage

To use the `core-loop` module, ensure that it is included as a dependency in your project’s module descriptor. 
You can access the exported packages and utilize the functionalities provided by this module.
*/
module main {
    requires llm;
}